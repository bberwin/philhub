{
  "buildCommand": "cd apps/landing && bun run build",
  "outputDirectory": "apps/landing/dist",
  "installCommand": "bun install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
