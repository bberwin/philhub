{
  "buildCommand": "turbo run build --filter=landing",
  "outputDirectory": "dist",
  "installCommand": "bun install",
  "framework": "vite",
  "rootDirectory": "apps/landing",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
